<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/fondo_pop_up_alarma"
    android:gravity="center"
    android:orientation="vertical">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginBottom="@dimen/activity_vertical_margin"
        android:src="@drawable/escudo_lobo_negativo"/>

    <EditText
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:id="@+id/add_new_alarm"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:hint="Código de alarma"
        android:imeOptions="actionNext"
        android:inputType="number"
        android:maxLines="1"
        android:textColorHint="#fff"
        android:singleLine="true"
        android:background="@drawable/input_layout_blanco">
        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/add_new_alarm_name"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:hint="Nombre de Alarma"
        android:imeOptions="actionDone"
        android:inputType="textCapWords"
        android:maxLines="1"
        android:textColorHint="#fff"
        android:singleLine="true"
        android:background="@drawable/input_layout_blanco">
    </EditText>

    <EditText
        android:id="@+id/add_new_service"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:hint="Código de Servicio"
        android:maxLines="1"
        android:textColorHint="#fff"
        android:singleLine="true"
        android:background="@drawable/input_layout_blanco"
        android:editable="false"
        android:visibility="gone">
    </EditText>

    <LinearLayout
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">
        <Button
            android:id="@+id/btn_new_alarm_save"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/btn_blanco"
            android:layout_weight="1"
            android:textColor="#3F77BE"
            android:textStyle="bold"
            android:text="Agregar"/>

        <Button
            android:id="@+id/btn_new_alarm_cancel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/btn_blanco"
            android:layout_weight="1"
            android:textColor="#3F77BE"
            android:textStyle="bold"
            android:text="Cancelar"/>
    </LinearLayout>
</LinearLayout>